# Historique des migrations 001–033

Les fichiers historiques `001_*.sql` à `033_*.sql` restent dans ce dossier
afin de préserver les tests utiles et la
traçabilité Git. Le runner les ignore : ils ne servent plus à initialiser une
nouvelle base et ne doivent jamais être rejoués en bloc.

La prochaine évolution structurelle porte obligatoirement le numéro `034`.
