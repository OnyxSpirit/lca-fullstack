import type { RowDataPacket } from 'mysql2/promise';
import { execute, query } from '../../config/database.js';
import { emitToUser } from '../../realtime/socket.js';
export type NotificationPriority='low'|'normal'|'high'|'urgent';
export interface CreateNotificationInput{userId:string;subject:string;message:string;eventType?:string|null;referenceType?:string|null;referenceId?:string|null;priority?:NotificationPriority;channel?:'notification'|'email'|'sms';eventKey?:string|null}
export async function createUserNotification(input:CreateNotificationInput){const[active]=await query<RowDataPacket[]>('SELECT id FROM users WHERE id=? AND is_active=TRUE',[input.userId]);if(!active)return null;const channel=input.channel??'notification',result=await execute(`INSERT IGNORE INTO notifications(user_id,channel,subject,message,status,delivery_status,sent_at,reference_type,reference_id,event_type,priority,event_key) VALUES(?,?,?,?,?,'sent',NOW(),?,?,?,?,?)`,[input.userId,channel,input.subject,input.message,'sent',input.referenceType??null,input.referenceId??null,input.eventType??null,input.priority??'normal',input.eventKey??null]);if(!result.affectedRows)return null;const record={id:String(result.insertId),userId:input.userId,subject:input.subject,message:input.message,eventType:input.eventType??null,referenceType:input.referenceType??null,referenceId:input.referenceId??null,priority:input.priority??'normal',createdAt:new Date().toISOString(),readAt:null};if(channel==='notification')emitToUser(input.userId,'notifications:created',record);return record}
export interface NotifyRolesInput extends Omit<CreateNotificationInput,'userId'>{agencyId:string;roles:string[];excludeUserIds?:string[];includeGlobalRoles?:string[]}
export async function notifyRoles(input:NotifyRolesInput){const excluded=new Set((input.excludeUserIds??[]).map(String)),globals=input.includeGlobalRoles?.length?input.includeGlobalRoles:['__NONE__'],roleMarks=input.roles.map(()=>'?').join(','),globalMarks=globals.map(()=>'?').join(','),users=await query<RowDataPacket[]>(`SELECT DISTINCT u.id FROM users u JOIN user_roles ur ON ur.user_id=u.id JOIN roles r ON r.id=ur.role_id WHERE u.is_active=TRUE AND ((u.agency_id=? AND r.code IN (${roleMarks})) OR r.code IN (${globalMarks}))`,[input.agencyId,...input.roles,...globals]);let created=0;for(const user of users){const userId=String(user.id);if(excluded.has(userId))continue;try{if(await createUserNotification({...input,userId,eventKey:input.eventKey?`${input.eventKey}:user:${userId}`:null}))created++}catch(error){console.error('Notification non créée',error)}}return created}
