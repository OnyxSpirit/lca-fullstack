import type{Request}from'express';import type{PoolConnection,ResultSetHeader,RowDataPacket}from'mysql2/promise';import{query,transaction}from'../../config/database.js';import{HttpError}from'../../shared/http-error.js';import{emitToAgenciesAndGlobals,emitToAgencyAndGlobals}from'../../realtime/socket.js';import{getBusinessIdentity,getEffectiveBusinessSettings}from'./setting-resolver.js';import{SETTING_KEYS,type AgencyPayload,type ConcessionIdentityPayload,type UpdateSettingsPayload}from'./setting.types.js';
const id=(v:unknown)=>{const x=String(v??'');if(!/^[1-9]\d*$/.test(x))throw new HttpError(400,'Identifiant invalide');return x},text=(v:unknown,label:string,required=false,max=255)=>{const x=typeof v==='string'?v.trim():'';if(required&&!x)throw new HttpError(400,`${label} obligatoire`);if(x.length>max)throw new HttpError(400,`${label} trop long`);return x||null},number=(v:unknown,label:string,max=Number.MAX_SAFE_INTEGER)=>{if(typeof v!=='number'||!Number.isFinite(v)||v<0||v>max)throw new HttpError(400,`${label} invalide`);return v};
async function audit(c:PoolConnection,r:Request,type:string,entityId:string,action:string,oldValues:unknown,newValues:unknown){await c.execute('INSERT INTO audit_logs(user_id,module,entity_type,entity_id,action,old_values,new_values,ip_address,user_agent) VALUES(?,?,?,?,?,?,?,?,?)',[r.user!.sub,'settings',type,entityId,action,JSON.stringify(oldValues),JSON.stringify(newValues),r.ip??null,r.get('user-agent')??null])}
async function currentConcessionId(r:Request){const[row]=await query<RowDataPacket[]>('SELECT concession_id FROM agencies WHERE id=?',[r.user!.agencyId]);if(!row)throw new HttpError(400,'Agence de rattachement invalide');return String(row.concession_id)}
async function emitConcessionSettings(concessionId:string){const agencies=await query<RowDataPacket[]>('SELECT id FROM agencies WHERE concession_id=?',[concessionId]);emitToAgenciesAndGlobals(agencies.map(x=>String(x.id)),'settings:updated',{concessionId})}
async function manageableAgency(agencyId:string,r:Request){const[row]=await query<RowDataPacket[]>('SELECT id,concession_id FROM agencies WHERE id=?',[agencyId]);if(!row)throw new HttpError(404,'Agence introuvable');if(!r.user!.roles.includes('SUPER_ADMIN')&&String(row.concession_id)!==await currentConcessionId(r))throw new HttpError(403,'Agence hors de votre concession');return row}
export async function get(r:Request){const concessionId=await currentConcessionId(r),[c]=await query<RowDataPacket[]>('SELECT * FROM concessions WHERE id=?',[concessionId]);if(!c)throw new HttpError(404,'Concession introuvable');const settings=await getEffectiveBusinessSettings(r.user!.agencyId!);return{concession:{id:String(c.id),name:c.name,legalName:c.legal_name,taxIdentifier:c.tax_identifier,address:c.address,city:c.city,country:c.country,currencyCode:c.currency_code,timezone:c.timezone},billing:{defaultVatRate:settings.vatRate},workshop:{rates:settings.rates}}}
export function validateSettings(body:unknown):UpdateSettingsPayload{if(!body||typeof body!=='object'||Array.isArray(body))throw new HttpError(400,'Configuration invalide');const b=body as Partial<UpdateSettingsPayload>,vat=number(b.billing?.defaultVatRate,'TVA',100),rates=b.workshop?.rates;if(!rates)throw new HttpError(400,'Tarifs atelier obligatoires');return{billing:{defaultVatRate:vat},workshop:{rates:{T1:number(rates.T1,'Tarif T1'),T2:number(rates.T2,'Tarif T2'),T3:number(rates.T3,'Tarif T3'),T4:number(rates.T4,'Tarif T4')}}}}
export async function update(body:unknown,r:Request){const value=validateSettings(body),concessionId=await currentConcessionId(r),entries=[[SETTING_KEYS.vat,value.billing.defaultVatRate],[SETTING_KEYS.T1,value.workshop.rates.T1],[SETTING_KEYS.T2,value.workshop.rates.T2],[SETTING_KEYS.T3,value.workshop.rates.T3],[SETTING_KEYS.T4,value.workshop.rates.T4]]as const;await transaction(async c=>{const[old]=await c.execute<RowDataPacket[]>(`SELECT setting_key,setting_value FROM settings WHERE scope_type='concession' AND scope_id=? AND setting_key IN (?,?,?,?,?)`,[concessionId,...entries.map(x=>x[0])]);for(const[key,v]of entries)await c.execute(`INSERT INTO settings(scope_type,scope_id,setting_key,setting_value,description,updated_by) VALUES('concession',?,?,?,?,?) ON DUPLICATE KEY UPDATE setting_value=VALUES(setting_value),updated_by=VALUES(updated_by)`,[concessionId,key,JSON.stringify(v),key,r.user!.sub]);await audit(c,r,'concession',concessionId,'settings.updated',Object.fromEntries(old.map(x=>[x.setting_key,x.setting_value])),Object.fromEntries(entries))});await emitConcessionSettings(concessionId);return get(r)}
export async function currentConcession(r:Request){return getBusinessIdentity(r.user!.agencyId!)}
export function validateConcession(body:unknown):ConcessionIdentityPayload{const b=(body??{})as Partial<ConcessionIdentityPayload>,currency=String(text(b.currencyCode,'Devise',true,3)).toUpperCase(),timezone=text(b.timezone,'Fuseau horaire',true,80)!;if(!/^[A-Z]{3}$/.test(currency))throw new HttpError(400,'Devise invalide');try{Intl.DateTimeFormat('fr',{timeZone:timezone})}catch{throw new HttpError(400,'Fuseau horaire invalide')}return{name:text(b.name,'Nom commercial',true,150)!,legalName:text(b.legalName,'Raison sociale',false,200),taxIdentifier:text(b.taxIdentifier,'Identifiant fiscal',false,100),address:text(b.address,'Adresse',false,500),city:text(b.city,'Ville',false,100),country:text(b.country,'Pays',false,100),currencyCode:currency,timezone}}
export async function updateConcession(body:unknown,r:Request){const v=validateConcession(body),concessionId=await currentConcessionId(r);await transaction(async c=>{const[old]=await c.execute<RowDataPacket[]>('SELECT * FROM concessions WHERE id=? FOR UPDATE',[concessionId]);await c.execute('UPDATE concessions SET name=?,legal_name=?,tax_identifier=?,address=?,city=?,country=?,currency_code=?,timezone=? WHERE id=?',[v.name,v.legalName??null,v.taxIdentifier??null,v.address??null,v.city??null,v.country??null,v.currencyCode,v.timezone,concessionId]);await audit(c,r,'concession',concessionId,'concession.updated',old[0],v)});await emitConcessionSettings(concessionId);return currentConcession(r)}
export async function agencies(r:Request){const superAdmin=r.user!.roles.includes('SUPER_ADMIN'),director=r.user!.roles.includes('DIRECTOR'),concessionId=await currentConcessionId(r),rows=await query<RowDataPacket[]>(`SELECT a.id,a.concession_id,a.name,a.code,a.address,a.city,a.phone,a.email,a.is_active FROM agencies a WHERE (? OR (? AND a.concession_id=?) OR a.id=?) ORDER BY a.name`,[superAdmin,director,concessionId,r.user!.agencyId]);return rows.map(x=>({id:String(x.id),concessionId:String(x.concession_id),name:x.name,code:x.code,address:x.address,city:x.city,phone:x.phone,email:x.email,isActive:Boolean(x.is_active)}))}
export function validateAgency(body:unknown,creating=false):AgencyPayload{const b=(body??{})as Partial<AgencyPayload>;return{name:text(b.name,'Nom agence',creating,150)??'',code:String(text(b.code,'Code agence',creating,50)??'').toUpperCase(),address:text(b.address,'Adresse',false,500),city:text(b.city,'Ville',false,100),phone:text(b.phone,'Téléphone',false,50),email:text(b.email,'Email',false,150),concessionId:b.concessionId?id(b.concessionId):undefined}}
export async function createAgency(body:unknown,r:Request){const v=validateAgency(body,true),ownConcessionId=await currentConcessionId(r),concessionId=r.user!.roles.includes('SUPER_ADMIN')&&v.concessionId?v.concessionId:ownConcessionId;const agencyId=await transaction(async c=>{const[result]=await c.execute<ResultSetHeader>('INSERT INTO agencies(concession_id,name,code,address,city,phone,email,is_active) VALUES(?,?,?,?,?,?,?,TRUE)',[concessionId,v.name,v.code,v.address??null,v.city??null,v.phone??null,v.email??null]);await audit(c,r,'agency',String(result.insertId),'agency.created',null,v);return String(result.insertId)});emitToAgencyAndGlobals(r.user!.agencyId!,'agencies:updated',{agencyId});return{agencyId}}
export async function updateAgency(agencyId:string,body:unknown,r:Request){const v=validateAgency(body);await manageableAgency(agencyId,r);await transaction(async c=>{const[old]=await c.execute<RowDataPacket[]>('SELECT * FROM agencies WHERE id=? FOR UPDATE',[agencyId]);if(!old[0])throw new HttpError(404,'Agence introuvable');await c.execute('UPDATE agencies SET name=COALESCE(?,name),code=COALESCE(NULLIF(?,\'\'),code),address=?,city=?,phone=?,email=? WHERE id=?',[v.name||null,v.code,v.address??null,v.city??null,v.phone??null,v.email??null,agencyId]);await audit(c,r,'agency',agencyId,'agency.updated',old[0],v)});emitToAgencyAndGlobals(r.user!.agencyId!,'agencies:updated',{agencyId});return{agencyId}}
export async function agencyStatus(agencyId:string,value:unknown,r:Request){if(typeof value!=='boolean')throw new HttpError(400,'Statut agence invalide');await manageableAgency(agencyId,r);await transaction(async c=>{const[old]=await c.execute<RowDataPacket[]>('SELECT * FROM agencies WHERE id=? FOR UPDATE',[agencyId]);if(!old[0])throw new HttpError(404,'Agence introuvable');if(!value){const[users]=await c.execute<RowDataPacket[]>('SELECT COUNT(*) total FROM users WHERE agency_id=? AND is_active=TRUE',[agencyId]);if(Number(users[0]?.total)>0)throw new HttpError(409,`Cette agence possède encore ${users[0]!.total} utilisateurs actifs. Veuillez les transférer ou les désactiver avant de fermer l’agence.`);const[active]=await c.execute<RowDataPacket[]>(`SELECT (SELECT COUNT(*) FROM repair_orders WHERE agency_id=? AND status NOT IN('closed','cancelled'))+(SELECT COUNT(*) FROM deliveries WHERE agency_id=? AND status NOT IN('delivered','cancelled'))+(SELECT COUNT(*) FROM vehicles WHERE agency_id=? AND status NOT IN('sold','delivered')) total`,[agencyId,agencyId,agencyId]);if(Number(active[0]?.total)>0)throw new HttpError(409,`Cette agence possède encore ${active[0]!.total} opérations actives.`)}await c.execute('UPDATE agencies SET is_active=? WHERE id=?',[value,agencyId]);await audit(c,r,'agency',agencyId,value?'agency.reactivated':'agency.disabled',{isActive:Boolean(old[0].is_active)},{isActive:value})});emitToAgencyAndGlobals(r.user!.agencyId!,'agencies:updated',{agencyId,isActive:value});return{agencyId,isActive:value}}
