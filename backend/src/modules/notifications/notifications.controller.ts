import { Controller,Delete,Get,Param,Patch } from '@nestjs/common';import { ApiBearerAuth,ApiTags } from '@nestjs/swagger';import { AuthUser,CurrentUser } from '../../common/decorators/current-user.decorator';import { NotificationsService } from './notifications.service';@ApiTags('notifications')@ApiBearerAuth()@Controller('notifications')export class NotificationsController{constructor(private readonly s:NotificationsService){}@Get()list(@CurrentUser()u:AuthUser){return this.s.list(u.sub)}@Patch('read-all')all(@CurrentUser()u:AuthUser){return this.s.readAll(u.sub)}@Patch(':id/read')read(@Param('id')id:string,@CurrentUser()u:AuthUser){return this.s.read(id,u.sub)}@Delete(':id')remove(@Param('id')id:string,@CurrentUser()u:AuthUser){return this.s.remove(id,u.sub)}}
