import { Body,Controller,Get,Param,Post } from '@nestjs/common';import { ApiBearerAuth,ApiTags } from '@nestjs/swagger';import { AuthUser,CurrentUser } from '../../common/decorators/current-user.decorator';import { CreateQuotationDto } from './quotations.dto';import { QuotationsService } from './quotations.service';@ApiTags('quotations')@ApiBearerAuth()@Controller('quotations')export class QuotationsController{constructor(private readonly s:QuotationsService){}@Get()all(){return this.s.list()}@Get(':id')one(@Param('id')id:string){return this.s.detail(id)}@Post()create(@Body()d:CreateQuotationDto,@CurrentUser()u:AuthUser){return this.s.create(d,u.sub)}}
