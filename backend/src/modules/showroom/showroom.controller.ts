import { Body,Controller,Get,Param,Patch,Post } from '@nestjs/common';import { ApiBearerAuth,ApiTags } from '@nestjs/swagger';import { CreateVisitDto,VisitStatusDto } from './showroom.dto';import { ShowroomService } from './showroom.service';@ApiTags('showroom')@ApiBearerAuth()@Controller('showroom')export class ShowroomController{constructor(private readonly s:ShowroomService){}@Get()all(){return this.s.list()}@Post()create(@Body()d:CreateVisitDto){return this.s.create(d)}@Patch(':id/status')status(@Param('id')id:string,@Body()d:VisitStatusDto){return this.s.status(id,d.status)}}
