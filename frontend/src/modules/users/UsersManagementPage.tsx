import React,{useEffect,useMemo,useState}from'react';
import{Camera,KeyRound,Plus,Search,ShieldCheck,UserCheck,UserX}from'lucide-react';
import{Navigate}from'react-router-dom';
import{useAgenciesQuery}from'../../api/erpHooks';
import{type AdminUser,type CreateUserPayload,useRolePermissionsQuery,useRolesQuery,useUserActions,useUsersAdminQuery}from'../../api/userHooks';
import{PageHeader}from'../../components/common/PageHeader';
import{Badge}from'../../components/ui/Badge';
import{Button}from'../../components/ui/Button';
import{Card}from'../../components/ui/Card';
import{Modal}from'../../components/ui/Modal';
import{assetUrl}from'../../services/apiClient';
import{useAuthStore}from'../../stores/authStore';
import{useUiStore}from'../../stores/uiStore';

const empty:CreateUserPayload={firstName:'',lastName:'',email:'',phone:'',jobTitle:'',agencyId:'',roles:[],password:''};
const input='w-full rounded-md border border-slate-300 bg-white px-3 py-2 text-xs';
export const UsersManagementPage:React.FC=()=>{
 const auth=useAuthStore(),toast=useUiStore(s=>s.addToast),currentRoles=auth.currentUser?.roles?.length?auth.currentUser.roles:auth.currentUser?[auth.currentUser.role]:[],allowed=currentRoles.some(r=>['SUPER_ADMIN','DIRECTION'].includes(r));
 const[tab,setTab]=useState<'users'|'matrix'>('users'),[search,setSearch]=useState(''),[active,setActive]=useState<''|'true'|'false'>(''),[agencyId,setAgencyId]=useState(''),[role,setRole]=useState(''),[open,setOpen]=useState(false),[editing,setEditing]=useState<AdminUser|null>(null),[reset,setReset]=useState<AdminUser|null>(null),[form,setForm]=useState<CreateUserPayload>(empty),[selectedRole,setSelectedRole]=useState('');
 const users=useUsersAdminQuery({search,active,agencyId,role}),agencies=useAgenciesQuery(),roles=useRolesQuery(),permissions=useRolePermissionsQuery(selectedRole),actions=useUserActions();
 useEffect(()=>{if(!selectedRole&&roles.data?.[0])setSelectedRole(String(roles.data[0].id))},[roles.data,selectedRole]);
 const availableRoles=useMemo(()=>roles.data?.filter(r=>currentRoles.includes('SUPER_ADMIN')||!['SUPER_ADMIN','DIRECTOR'].includes(r.code))??[],[roles.data,currentRoles]);
 if(!allowed)return <Navigate to="/dashboard" replace/>;
 const show=(user?:AdminUser)=>{setEditing(user??null);setForm(user?{firstName:user.firstName,lastName:user.lastName,email:user.email,phone:user.phone??'',jobTitle:user.jobTitle??'',agencyId:user.agencyId,roles:user.roles,password:''}:{...empty,agencyId:agencies.data?.find(a=>a.isActive)?.id??'',roles:availableRoles[0]?[availableRoles[0].code]:[]});setOpen(true)};
 const submit=async(e:React.FormEvent)=>{e.preventDefault();try{if(editing)await actions.update.mutateAsync({id:editing.id,body:{...form,password:undefined} as never});else await actions.create.mutateAsync(form);toast({type:'success',title:editing?'Collaborateur modifié':'Collaborateur créé',description:'Les informations ont été enregistrées.'});setOpen(false)}catch(error){toast({type:'error',title:'Action impossible',description:error instanceof Error?error.message:'Erreur API'})}};
 const toggle=async(u:AdminUser)=>{if(u.isActive&&!confirm(`Désactiver cet agent ?\n\nIl ne pourra plus se connecter ni recevoir de nouvelles affectations. Son historique restera conservé.`))return;try{await actions.status.mutateAsync({id:u.id,isActive:!u.isActive});toast({type:'success',title:u.isActive?'Agent désactivé':'Agent réactivé',description:u.fullName})}catch(e){toast({type:'error',title:'Changement impossible',description:e instanceof Error?e.message:'Erreur API'})}};
 const upload=async(u:AdminUser,file?:File)=>{if(!file)return;try{await actions.avatar.mutateAsync({id:u.id,file});toast({type:'success',title:'Photo mise à jour',description:u.fullName})}catch(e){toast({type:'error',title:'Photo refusée',description:e instanceof Error?e.message:'Erreur API'})}};
 return <div className="space-y-6"><PageHeader title="Utilisateurs & habilitations" subtitle="Comptes, agences, rôles et accès opérationnels." breadcrumbs={[{label:'Accueil',href:'/dashboard'},{label:'Administration'},{label:'Utilisateurs'}]} actions={<Button size="sm" icon={<Plus className="w-4 h-4"/>} onClick={()=>show()}>Ajouter un collaborateur</Button>}/>
 <div className="flex gap-2 border-b"><button className={`px-4 py-2 text-xs font-bold ${tab==='users'?'border-b-2 border-[#8f1722]':''}`} onClick={()=>setTab('users')}>Collaborateurs</button><button className={`px-4 py-2 text-xs font-bold ${tab==='matrix'?'border-b-2 border-[#8f1722]':''}`} onClick={()=>setTab('matrix')}>Matrice RBAC</button></div>
 {tab==='users'?<><Card><div className="grid gap-3 md:grid-cols-4"><label className="relative"><Search className="absolute left-3 top-2.5 h-4 w-4 text-slate-400"/><input className={`${input} pl-9`} value={search} onChange={e=>setSearch(e.target.value)} placeholder="Nom ou e-mail"/></label><select className={input} value={active} onChange={e=>setActive(e.target.value as typeof active)}><option value="">Tous les statuts</option><option value="true">Actifs</option><option value="false">Inactifs</option></select><select className={input} value={agencyId} onChange={e=>setAgencyId(e.target.value)}><option value="">Toutes les agences</option>{agencies.data?.filter(a=>a.isActive).map(a=><option key={a.id} value={a.id}>{a.name}</option>)}</select><select className={input} value={role} onChange={e=>setRole(e.target.value)}><option value="">Tous les rôles</option>{roles.data?.map(r=><option key={r.id} value={r.code}>{r.name}</option>)}</select></div></Card>
 <Card padding="none"><div className="overflow-x-auto"><table className="w-full text-left text-xs"><thead className="border-b bg-slate-50 text-slate-500"><tr><th className="p-4">Collaborateur</th><th className="p-4">Rôles</th><th className="p-4">Agence</th><th className="p-4">Statut</th><th className="p-4 text-right">Actions</th></tr></thead><tbody>{users.data?.map(u=><tr key={u.id} className="border-b last:border-0"><td className="p-4"><div className="flex items-center gap-3">{u.avatar?<img src={assetUrl(u.avatar)} className="h-9 w-9 rounded-full object-cover"/>:<div className="flex h-9 w-9 items-center justify-center rounded-full bg-[#161618] font-bold text-white">{u.firstName[0]}{u.lastName[0]}</div>}<div><div className="font-bold">{u.fullName}</div><div className="text-slate-500">{u.email}{u.jobTitle?` · ${u.jobTitle}`:''}</div></div></div></td><td className="p-4"><div className="flex flex-wrap gap-1">{u.roles.map(r=><Badge key={r} size="sm">{r}</Badge>)}</div></td><td className="p-4">{u.agencyName}</td><td className="p-4"><Badge variant={u.isActive?'success':'default'}>{u.isActive?'Actif':'Inactif'}</Badge></td><td className="p-4"><div className="flex justify-end gap-1"><Button size="sm" variant="outline" onClick={()=>show(u)}>Modifier</Button><label className="cursor-pointer rounded-md border p-2" title="Photo"><Camera className="h-4 w-4"/><input hidden type="file" accept="image/jpeg,image/png,image/webp" onChange={e=>void upload(u,e.target.files?.[0])}/></label>{u.avatar&&<Button size="sm" variant="outline" onClick={async()=>{try{await actions.deleteAvatar.mutateAsync(u.id)}catch(e){toast({type:"error",title:"Suppression impossible",description:e instanceof Error?e.message:"Erreur API"})}}}>Retirer photo</Button>}<Button size="sm" variant="outline" icon={<KeyRound className="h-4 w-4"/>} onClick={()=>setReset(u)}>Mot de passe</Button><Button size="sm" variant={u.isActive?'danger':'outline'} icon={u.isActive?<UserX className="h-4 w-4"/>:<UserCheck className="h-4 w-4"/>} disabled={u.id===auth.currentUser?.id&&u.isActive} onClick={()=>void toggle(u)}>{u.isActive?'Désactiver':'Réactiver'}</Button></div></td></tr>)}</tbody></table>{users.isLoading&&<p className="p-5 text-sm text-slate-500">Chargement…</p>}{users.error&&<p className="p-5 text-sm text-red-700">{users.error.message}</p>}</div></Card></>:<Card><div className="mb-5 flex items-center gap-3"><ShieldCheck className="h-5 w-5 text-[#8f1722]"/><select className={input} value={selectedRole} onChange={e=>setSelectedRole(e.target.value)}>{roles.data?.map(r=><option key={r.id} value={r.id}>{r.name} ({r.code})</option>)}</select></div><p className="mb-4 text-xs text-slate-500">Vue en lecture seule des permissions enregistrées. Les routes Express conservent leurs contrôles serveur explicites.</p><div className="overflow-x-auto"><table className="w-full text-xs"><thead><tr className="border-b bg-slate-50"><th className="p-3 text-left">Module</th><th className="p-3 text-left">Action</th><th className="p-3 text-left">Code</th></tr></thead><tbody>{permissions.data?.permissions?.map(p=><tr key={p.id} className="border-b"><td className="p-3 font-semibold">{p.module}</td><td className="p-3">{p.action}</td><td className="p-3 text-slate-500">{p.code}</td></tr>)}</tbody></table></div></Card>}
 <Modal isOpen={open} onClose={()=>setOpen(false)} title={editing?'Modifier le collaborateur':'Créer un collaborateur'}><form className="space-y-3" onSubmit={submit}><div className="grid grid-cols-2 gap-3"><label className="text-xs">Prénom(s) *<input className={input} required value={form.firstName} onChange={e=>setForm({...form,firstName:e.target.value})}/></label><label className="text-xs">Nom *<input className={input} required value={form.lastName} onChange={e=>setForm({...form,lastName:e.target.value})}/></label></div><label className="block text-xs">E-mail *<input className={input} type="email" required value={form.email} onChange={e=>setForm({...form,email:e.target.value})}/></label><div className="grid grid-cols-2 gap-3"><label className="text-xs">Téléphone<input className={input} value={form.phone} onChange={e=>setForm({...form,phone:e.target.value})}/></label><label className="text-xs">Fonction<input className={input} value={form.jobTitle} onChange={e=>setForm({...form,jobTitle:e.target.value})}/></label></div><label className="block text-xs">Agence *<select className={input} required value={form.agencyId} onChange={e=>setForm({...form,agencyId:e.target.value})}><option value="" disabled>Choisir une agence</option>{agencies.data?.filter(a=>a.isActive).map(a=><option key={a.id} value={a.id}>{a.name}</option>)}</select></label><fieldset><legend className="text-xs">Rôles *</legend><div className="grid grid-cols-2 gap-2 rounded-md border p-3">{availableRoles.map(r=><label key={r.id} className="text-xs"><input type="checkbox" checked={form.roles.includes(r.code)} onChange={e=>setForm({...form,roles:e.target.checked?[...form.roles,r.code]:form.roles.filter(x=>x!==r.code)})}/> {r.name}</label>)}</div></fieldset>{!editing&&<label className="block text-xs">Mot de passe initial *<input className={input} type="password" minLength={8} required value={form.password} onChange={e=>setForm({...form,password:e.target.value})}/></label>}<div className="flex justify-end gap-2"><Button type="button" variant="outline" onClick={()=>setOpen(false)}>Annuler</Button><Button type="submit" disabled={!form.roles.length}>Enregistrer</Button></div></form></Modal>
 <Modal isOpen={Boolean(reset)} onClose={()=>setReset(null)} title="Réinitialiser le mot de passe"><form className="space-y-4" onSubmit={async e=>{e.preventDefault();const value=String(new FormData(e.currentTarget).get('password'));try{await actions.resetPassword.mutateAsync({id:reset!.id,newPassword:value});toast({type:'success',title:'Mot de passe réinitialisé',description:'Toutes les sessions ont été révoquées.'});setReset(null)}catch(error){toast({type:'error',title:'Réinitialisation impossible',description:error instanceof Error?error.message:'Erreur API'})}}}><p className="text-xs text-slate-500">Compte : {reset?.fullName}</p><input className={input} name="password" type="password" minLength={8} required placeholder="Nouveau mot de passe"/><div className="flex justify-end gap-2"><Button type="button" variant="outline" onClick={()=>setReset(null)}>Annuler</Button><Button type="submit">Réinitialiser</Button></div></form></Modal></div>
}
