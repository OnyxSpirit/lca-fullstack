import{apiDownload}from'./apiClient';export async function openBusinessPdf(type:'sale'|'delivery'|'repair_order'|'vehicle',id:string,download=false){const blob=await apiDownload(`/documents/business/${type}/${id}/pdf${download?'?download=true':''}`);const url=URL.createObjectURL(blob);if(download){const a=document.createElement('a');a.href=url;a.download=`${type}-${id}.pdf`;a.click()}else window.open(url,'_blank','noopener,noreferrer');setTimeout(()=>URL.revokeObjectURL(url),60000)}
