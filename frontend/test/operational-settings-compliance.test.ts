import assert from'node:assert/strict';import{readFileSync}from'node:fs';import test from'node:test';const source=(p:string)=>readFileSync(new URL(`../${p}`,import.meta.url),'utf8');
test('Paramètres expose Fournisseurs et Ressources Atelier',()=>{const page=source('src/modules/settings/SettingsPage.tsx');assert.match(page,/Fournisseurs/);assert.match(page,/BaysSettings/);assert.match(page,/Atelier & ponts/)});
test('fournisseurs utilisent TanStack Query et invalident les deux modales PR',()=>{const hooks=source('src/api/resourceSettingsHooks.ts'),parts=source('src/modules/parts/SparePartsPage.tsx');assert.match(hooks,/useQuery/);assert.match(hooks,/part-references/);assert.match(parts,/Fournisseur principal/);assert.match(parts,/Créer une commande fournisseur/)});
test('pont occupé non désactivable et remise SAV impayée désactivée',()=>{const settings=source('src/modules/settings/OperationalResourcesSettings.tsx'),sav=source('src/modules/service/RepairOrderDetailPage.tsx');assert.match(settings,/disabled=\{b\.occupied_now\}/);assert.match(sav,/balanceDue/);assert.match(sav,/facture atelier n’est pas soldée/)});
