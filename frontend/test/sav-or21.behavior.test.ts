import assert from'node:assert/strict';import{readFile}from'node:fs/promises';import test from'node:test';
test('SAV-OR-21 affiche autorisé, pointé et facturable sans ressaisie des heures',async()=>{const page=await readFile(new URL('../src/modules/service/RepairOrderDetailPage.tsx',import.meta.url),'utf8');assert.match(page,/Autorisé, pointé et facturable restent trois valeurs indépendantes/);assert.match(page,/item\.quantity.*item\.actualHours/s);assert.match(page,/createEstimateIntervention/);assert.match(page,/estimateItemId:item\.id/);assert.match(page,/Intervention créée/);assert.doesNotMatch(page,/plannedHours:10/);});
